apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
data:
  # Local users declare karo yahan
  accounts.dev-user: apiKey, login
  accounts.st-user: apiKey, login

  repositories: |
    - url: https://github.com/sahu04/agaro_cd.git